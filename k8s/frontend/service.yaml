#
# GreenCity Frontend Service
#
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: greencity
  labels:
    app: frontend
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
